# app/__init__.py

# This is an empty file, but it's necessary for Python to recognize the "app" folder as a package.
# We can also put initialization code in this file if needed.

# Import submodules so that they can be accessed directly from the package namespace
from . import database
from . import houseparser